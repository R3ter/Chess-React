import React from "react";
import Board from "./Board";

const Game = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
      }}
    >
      <Board></Board>;
    </div>
  );
};

export default Game;
